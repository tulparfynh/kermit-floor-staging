// This file is intentionally left blank. 
// A static sitemap.xml in the /public directory is used instead.
// The presence of a sitemap.ts file, even if empty, can cause conflicts.
// This export prevents Next.js from treating this as a sitemap generation file.
export const ignored = true;
